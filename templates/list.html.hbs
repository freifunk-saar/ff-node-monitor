{{~#*inline "title"~}}
  Knotenliste für {{form.email}}
{{~/inline~}}
{{~#*inline "page"}}
  {{#each nodes}}
  <div>
    <form method="post" action="prepare_action">
      <span class="node" data-node-id="{{this.node}}">{{this.node}}</span>
      <input type="hidden" name="email" value="{{this.email}}">
      <input type="hidden" name="op" value="remove">
      <input type="hidden" name="node" value="{{this.node}}">
      <input type="submit" value="[x]" class="link">
    </form>
  </div>
  {{else}}
  <p>Du überwachst bisher keinen Knoten.</p>
  {{/each}}

  <h3>Knoten hinzufügen</h3>
  <form method="post" action="prepare_action">
    <input type="hidden" name="email" value="{{form.email}}">
    <input type="hidden" name="op" value="add">
    Knoten ID: <input type="text" name="node">
    <input type="submit" value="Hinzufügen">
  </form>
{{~/inline}}
{{~> partials/page }}
